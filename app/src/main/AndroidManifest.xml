<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.project.mengenalbagiantumbuhan">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.ACCESS_WIFI_STATE" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.MengenalBagianTumbuhan">
        <activity android:name=".ui.latihan.LatihanActivity"></activity>
        <activity android:name=".ui.video.VideoActivity" />
        <activity android:name=".ui.games.DetailGamesActivity" />
        <activity android:name=".ui.games.GamesActivity" />
        <activity android:name=".ui.quiz.ResultActivity" />
        <activity android:name=".ui.quiz.StartActivity" />
        <activity android:name=".ui.quiz.QuizActivity" />
        <activity android:name=".ui.detail.DetailActivity" />
        <activity android:name=".ui.splash.SplashActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".ui.main.MainActivity" />
    </application>

</manifest>